import '../../naro_exporter.dart';

class NaroSettings extends ConsumerWidget {
  const NaroSettings({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}