import '../../naro_exporter.dart';

class MyActivities extends ConsumerWidget {
  const MyActivities({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}